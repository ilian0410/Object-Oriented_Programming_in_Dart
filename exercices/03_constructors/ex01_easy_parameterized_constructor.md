# Constructors — Easy

## Instructions

Create a `Song` class with:

- **Fields**: `title` (String), `artist` (String), `duration` (int, in seconds)
- **Parameterized constructor** using the `this.field` shorthand syntax
- **Method**: `play()` that prints `"Now playing: title by artist (duration seconds)"`

In `main()`, create two `Song` objects using the constructor and call `play()` on each.

## Expected Output

```
Now playing: Blinding Lights by The Weeknd (200 seconds)
Now playing: Shape of You by Ed Sheeran (233 seconds)
```

## Starter Code

```dart
class Song {
  // TODO: add fields
  // TODO: add parameterized constructor
  // TODO: add play method
}

void main() {
  // TODO: create two Song objects using the constructor
}
```
